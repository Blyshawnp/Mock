<UserControl x:Class="AppName.UI.Controls.LookupTableEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="900">
    <Border Padding="10"
            Background="#FFF7F7F7"
            BorderBrush="#FFDDDDDD"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0"
                       Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               FontSize="15"
                               Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    <TextBlock FontSize="11"
                               Foreground="DarkSlateGray"
                               Text="LookupItem editor only" />
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            DockPanel.Dock="Right">
                    <Button Margin="0,0,8,0" Content="Add" Click="AddButton_OnClick" />
                    <Button Margin="0,0,8,0" Content="Remove" Click="RemoveButton_OnClick" />
                    <Button Margin="0,0,8,0" Content="Up" Click="MoveUpButton_OnClick" />
                    <Button Content="Down" Click="MoveDownButton_OnClick" />
                </StackPanel>
            </DockPanel>

            <DataGrid x:Name="EditorGrid"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="2*" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="3*" />
                    <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                    <DataGridTextColumn Header="Sort" Binding="{Binding SortOrder, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
