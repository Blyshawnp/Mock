<UserControl x:Class="AppName.UI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Border Width="72" Height="36" Background="#FF0A1228" BorderBrush="#FF28447A" BorderThickness="1" CornerRadius="6" VerticalAlignment="Center">
                <TextBlock Text="ACD" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="18" />
            </Border>
            <StackPanel Margin="16,0,0,0">
                <TextBlock Style="{StaticResource PageTitleTextBlockStyle}"><Run Text="Welcome, "/><Run Text="{Binding WelcomeName}"/><Run Text="!"/></TextBlock>
                <TextBlock Style="{StaticResource SubtleTextBlockStyle}" Text="Mock Testing Suite — Certification" />
            </StackPanel>
        </StackPanel>

        <UniformGrid Grid.Row="1" Margin="0,20,0,0" Columns="3">
            <Border Style="{StaticResource DashboardCardStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Style="{StaticResource SubtleTextBlockStyle}" Text="TOTAL SESSIONS" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding TotalSessions}" FontSize="44" FontWeight="SemiBold" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DashboardCardStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Style="{StaticResource SubtleTextBlockStyle}" Text="PASS RATE" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding PassRate, StringFormat={}{0}%}" FontSize="44" FontWeight="SemiBold" Foreground="{StaticResource Brush.Success}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource DashboardCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SubtleTextBlockStyle}" Text="NC/NS RATE" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding NcnsRate, StringFormat={}{0}%}" FontSize="44" FontWeight="SemiBold" Foreground="{StaticResource Brush.Danger}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <UniformGrid Grid.Row="2" Margin="190,22,190,16" Columns="3">
            <Button Style="{StaticResource PrimaryButtonStyle}" Content="🚀 Start New Session" Command="{Binding StartNewSessionCommand}" Margin="0,0,14,0" />
            <Button Style="{StaticResource SuccessButtonStyle}" Content="🔁 Supervisor Transfer Only" Command="{Binding SupervisorTransferOnlyCommand}" Margin="0,0,14,0" />
            <Button Style="{StaticResource SecondaryButtonStyle}" Content="📊 Session History" Command="{Binding SessionHistoryCommand}" />
        </UniformGrid>

        <StackPanel Grid.Row="3">
            <TextBlock Style="{StaticResource SectionHeaderTextBlockStyle}" Text="RECENT SESSIONS" Margin="0,8,0,10" />
            <Border Style="{StaticResource DashboardCardStyle}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ItemsControl ItemsSource="{Binding RecentSessions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="20,16" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="260" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="180" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SubtleTextBlockStyle}" Text="{Binding StartedAt, StringFormat={}{0:yyyy-MM-dd hh:mm tt}}" />
                                    <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding CandidateName}" />
                                    <Border Grid.Column="2" CornerRadius="14" Background="#1E2D486A" Padding="14,5" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Status}" Foreground="#FFA8C6FF" FontWeight="SemiBold" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
            <TextBlock Margin="0,12,0,0" Style="{StaticResource SubtleTextBlockStyle}" Text="{Binding StatusMessage}" />
        </StackPanel>
    </Grid>
</UserControl>
